#include <bits/stdc++.h>
using namespace std;
# define rep(i,n) for(int i=0;i<(n);i++)
typedef long long ll;

int main(){
	cin.tie(0);
   	ios::sync_with_stdio(false);
	
	ll N;
	cin >> N;
    typedef pair<ll,ll> P;
    vector<P> A(N),B(N),L(N),G(N);
    rep(i,N){
        ll a;
        cin>>a;
        A[i]=P{i,a};
    }
    rep(i,N){
        ll b;
        cin>>b;
        B[i]=P{i,b};
    }

    

    for(ll i=0;i<N;i++){

    }


	return 0;
}
